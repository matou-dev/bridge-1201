package net.minecraftforge.fml.event.lifecycle;

import net.minecraftforge.eventbus.api.Event;

/**
 * Registration compile stub: shape-only Forge 1.20.1-47.2.0 API
 * (fmlcore jar, never obfuscated). Never runs (compile classpath only).
 * The setup event is the init-time verify surface (see hub
 * decisions/REGISTRATION.md) — the live tranche pins it against the
 * provisioned 47.2.0 jars, drift fails loudly there, never silently
 * here.
 */
public class FMLCommonSetupEvent extends Event {
}
