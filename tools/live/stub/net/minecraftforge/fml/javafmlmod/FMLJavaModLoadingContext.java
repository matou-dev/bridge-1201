package net.minecraftforge.fml.javafmlmod;

import net.minecraftforge.eventbus.api.IEventBus;

/**
 * Registration compile stub: shape-only Forge 1.20.1-47.2.0 API
 * (fmlloader/javafmllanguage jars, never obfuscated). Never runs
 * (compile classpath only). Only the members {@code forge/} references
 * ({@code get}, {@code getModEventBus}) — the live tranche pins them
 * against the provisioned 47.2.0 jars, drift fails loudly there, never
 * silently here.
 */
public class FMLJavaModLoadingContext {
    public static FMLJavaModLoadingContext get() {
        return null;
    }

    public IEventBus getModEventBus() {
        return null;
    }
}
