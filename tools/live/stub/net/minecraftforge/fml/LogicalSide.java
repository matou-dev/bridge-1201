package net.minecraftforge.fml;

/**
 * D1 compile stub: shape-only Forge 1.20.1-47.2.0 API (fmlcore jar, never
 * obfuscated). Never runs (compile classpath only). Pinned by
 * tools/run-live.sh (D3) — drift fails loudly.
 */
public enum LogicalSide {
    CLIENT, SERVER
}
