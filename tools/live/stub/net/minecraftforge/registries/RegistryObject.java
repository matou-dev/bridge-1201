package net.minecraftforge.registries;

import net.minecraft.resources.ResourceLocation;

/**
 * Registration compile stub: shape-only Forge 1.20.1-47.2.0 API
 * (universal jar, never obfuscated). Never runs (compile classpath
 * only). Only the members {@code forge/} references ({@code get},
 * {@code getId}) — the live tranche pins them against the provisioned
 * 47.2.0 jars, drift fails loudly there, never silently here.
 */
public class RegistryObject<T> {
    public T get() {
        return null;
    }

    public ResourceLocation getId() {
        return null;
    }
}
