package net.minecraft.core;

/**
 * D1 compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Every member
 * is pinned to Forge 1.20.1-47.2.0 production (Mojmap classes, SRG members)
 * by tools/run-live.sh (D3) before compiling — drift fails loudly.
 */
public class BlockPos {
    public BlockPos(int x, int y, int z) {
    }
}
