package net.minecraft.world.level;

/**
 * Loot compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Only a
 * type: {@code BlockEvent.getLevel} returns this (measured via javap
 * against the pinned 47.2.0 universal — the 1.16.5 {@code IWorld} shape
 * does not port), the forge side narrows it to {@code ServerLevel}
 * before any read.
 */
public interface LevelAccessor {
}
