package net.minecraft.world.level;

/**
 * Loot compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Only a
 * type: the {@code ItemStack} carrier ctor takes this (measured via
 * javap against the pinned 47.2.0 bytes — the 1.16.5 {@code (Item,int)}
 * shape does not port), and {@code Item} implements it.
 */
public interface ItemLike {
}
