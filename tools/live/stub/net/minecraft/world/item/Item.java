package net.minecraft.world.item;

import net.minecraft.world.level.ItemLike;

/**
 * Loot compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Only a
 * type: {@code Items.DIAMOND} is one, and the carrier ctor takes its
 * {@code ItemLike} face (measured via javap against the pinned 47.2.0
 * bytes — {@code Item implements ItemLike}).
 */
public class Item implements ItemLike {
}
