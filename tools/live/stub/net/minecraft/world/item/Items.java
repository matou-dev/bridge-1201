package net.minecraft.world.item;

/**
 * Loot compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only).
 * {@code DIAMOND} is the vanilla carrier until item registration lands
 * (hub decisions/LOOT.md). Mojmap name measured, not recalled, against
 * the pinned 47.2.0 bytes (server.txt + joined.tsrg v2 + javap: public
 * static {@code Item} field). Pinned by tools/run-live.sh (narrow map) —
 * drift fails loudly. Object-typed, never a javac-folded constant.
 */
public class Items {
    public static final Item DIAMOND = null;
}
