package net.minecraft.world.entity.ai.attributes;

/**
 * Spawn compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). The hp
 * seam lands the spec value through {@code setBaseValue} on the instance
 * {@code LivingEntity.getAttribute} returns (measured via server.txt +
 * joined.tsrg v2 + javap, same practice). Pinned by tools/run-live.sh
 * (narrow map) -- drift fails loudly.
 */
public class AttributeInstance {
    public void setBaseValue(double value) {
    }
}
