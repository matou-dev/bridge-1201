package net.minecraft.world.entity.ai.attributes;

/**
 * Spawn compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). The hp
 * seam addresses {@code MAX_HEALTH} through this (measured via
 * server.txt: public static field). Non-final by design (registry-style
 * ref, Items.DIAMOND precedent). Pinned by tools/run-live.sh (narrow
 * map) -- drift fails loudly.
 */
public class Attributes {
    public static Attribute MAX_HEALTH;
}
