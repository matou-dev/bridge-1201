package net.minecraft.resources;

/**
 * D1 compile stub: shape-only 1.20.1 (Mojmap) vanilla API used by
 * {@code forge/} sources. Never runs (compile classpath only). Pinned by
 * tools/run-live.sh (D3) — drift fails loudly.
 *
 * <p>Companion shape (loot proof, DEV ONLY): the autoplay companion gates
 * dim-0 through {@code location} (measured via server.txt + joined.tsrg
 * v2 + javap, same practice — no vanilla field touched, the Mojmap
 * autoplay derive pins methods only). Pinned by the AUTOPLAY derive in
 * hub tools/run-client.sh (want.txt) — drift fails loudly.
 */
public class ResourceKey<T> {
    public ResourceLocation location() {
        return null;
    }
}
