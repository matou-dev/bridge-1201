package net.minecraft.client.renderer.entity;

import net.minecraft.world.entity.Entity;

/**
 * Custom-entity compile stub: shape-only 1.20.1 (Mojmap) vanilla client
 * API used by {@code forge/} sources. Never runs (compile classpath
 * only — client classes never load on a dedicated server; the renderer
 * subscriber holding them is dist-filtered to the client and never
 * referenced server-side). Only the supertype the pig renderer hangs
 * under. Client link unproven until the live tranche.
 */
public class EntityRenderer<T extends Entity> {
}
